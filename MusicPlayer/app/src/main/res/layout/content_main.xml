<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.user1.musicplayer.MainActivity"
    tools:showIn="@layout/activity_main"
    android:background="#EFECD3">
<!-- android:background="#007080" -->

    <Button
        android:layout_width="49dp"
        android:layout_height="49dp"
        android:background="@drawable/ic_skip_previous_black_24dp"
        android:id="@+id/skipPrev"
        android:layout_marginStart="99dp"
        android:layout_marginLeft="99dp"/>

    <ImageButton
        android:layout_width="49dp"
        android:layout_height="49dp"
        android:src="@drawable/ic_play_arrow_black_24dp"
        android:scaleX="2"
        android:scaleY="2"
        android:background="#EFECD3"
        android:id="@+id/playPause"
        android:layout_toRightOf="@id/skipPrev"
        android:layout_toEndOf="@id/skipPrev"/>

    <Button
        android:layout_width="49dp"
        android:layout_height="49dp"
        android:background="@drawable/ic_skip_next_black_24dp"
        android:id="@+id/skipNext"
        android:layout_toRightOf="@id/playPause"
        android:layout_toEndOf="@id/playPause"/>

    <Button
        android:layout_width="49dp"
        android:layout_height="49dp"
        android:background="@drawable/ic_stop_black_24dp"
        android:id="@+id/stop"/>

    <SeekBar
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:layout_below="@id/playPause"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:id="@+id/bar"/>

    <ListView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/bar"
        android:verticalSpacing="10dp"/>



</RelativeLayout>

